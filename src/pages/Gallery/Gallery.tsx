import { useEffect, useMemo, useRef, useState } from "react";
import "./Gallery.css";

type ConfettiShard = {
    id: number;
    x: number;
    y: number;
    r: number;
    s: number;
    d: number;
    dx: number;
    shape: "rect" | "heart" | "star" | "circle" | "sparkle";
    color: string;
};

export default function Gallery() {
    const scrollerRef = useRef<HTMLDivElement | null>(null);

    // UI states
    const [showChoices, setShowChoices] = useState(false);
    const [yesScale, setYesScale] = useState(1);
    const [noScale, setNoScale] = useState(1);
    const [noButtonVisible, setNoButtonVisible] = useState(true);

    // Scroll progress
    const [progress, setProgress] = useState(0);

    // confetti
    const [burst, setBurst] = useState<ConfettiShard[]>([]);
    const confettiCount = 52;

    // Single minimal palette around the accent
    const accent = "#c68e87";
    const accentPalette = useMemo(
        () => [accent, "#d2a49e", "#b47970", "#a86d64", "#8f5850", "#ff9bb3", "#ffb3c1", "#8ab6ff", "#9bd0ff", "#ffe08a",
            "#c8a2ff", "#a8e6cf", "#ffd3b6", "#ffaaa5", "#d8bfd8"],
        []
    );

    const makeBurst = useMemo(
        () => () => {
            const shapes: Array<"rect" | "heart" | "star" | "circle" | "sparkle"> =
                ["rect", "heart", "star", "circle", "sparkle"];

            const shards: ConfettiShard[] = Array.from({ length: confettiCount }).map((_, i) => ({
                id: Date.now() + i,
                x: Math.random() * 100,
                y: Math.random() * 10 + 5,
                r: Math.random() * 360,
                s: Math.random() * 0.7 + 0.6,
                d: Math.random() * 1.2 + 1.6,
                dx: (Math.random() - 0.5) * 22,
                shape: shapes[Math.floor(Math.random() * shapes.length)],
                color: accentPalette[Math.floor(Math.random() * accentPalette.length)]
            }));
            return shards;
        },
        [accentPalette]
    );

    // Manual scroll handler + show choices when reaching bottom
    const handleScroll = () => {
        const el = scrollerRef.current;
        if (!el) return;

        const max = el.scrollHeight - el.clientHeight || 1;
        setProgress(Math.min(1, el.scrollTop / max));

        const atBottom = Math.ceil(el.scrollTop + el.clientHeight + 1) >= el.scrollHeight;
        setShowChoices(atBottom);
    };

    // Initialize progress & choices on mount (in case content fits)
    useEffect(() => {
        const el = scrollerRef.current;
        if (!el) return;
        const atBottom = Math.ceil(el.scrollTop + el.clientHeight + 1) >= el.scrollHeight;
        setShowChoices(atBottom);
        const max = el.scrollHeight - el.clientHeight || 1;
        setProgress(Math.min(1, el.scrollTop / max));
    }, []);

    // Buttons logic
    const onClickNo = () => {
        const newNoScale = Math.max(0.3, noScale * 0.85);
        const newYesScale = Math.min(2.5, yesScale * 1.15);

        setNoScale(newNoScale);
        setYesScale(newYesScale);

        if (newNoScale <= 0.35) {
            setNoButtonVisible(false);
            if (navigator.vibrate) navigator.vibrate([50, 30, 50]);
        } else {
            if (navigator.vibrate) navigator.vibrate(15);
        }
    };

    const onClickYes = () => {
        if (navigator.vibrate) navigator.vibrate([12, 20, 12]);
        setBurst(makeBurst());

        setShowChoices(false);
        document.body.insertAdjacentHTML(
            "beforeend",
            `<div id="love-call" style="
            position:fixed; inset:0; display:flex; justify-content:center; align-items:center;
            background:rgba(0,0,0,.6); color:white; font-size:22px; font-weight:600; z-index:9999;">
            ☎️ กำลังโทรหาเค้าน้า 💗
        </div>`
        );

        setTimeout(() => {
            window.location.href = "tel:0970973258";
        }, 1000);
    };


    return (
        <div className="credits-page" style={{ backgroundColor: "#ddb892" }}>

            <div className="progress">
                <div className="progress-bar" style={{ width: `${progress * 100}%` }} />
            </div>

            <div
                ref={scrollerRef}
                className="credits-scroller"
                onScroll={handleScroll}
                role="region"
                aria-label="Love letter"
            >
                <div className="credits">
                    <p className="line">ก่อนจะถึงเดือนธันวาเค้าอยากจะทำอะไรให้เธอสักอย่างจริงๆ</p>
                    <p className="line">เค้าตั้งใจอยากจะให้สิ่งนี้เธอมากๆนะคะ ให้ดอกไม้คือเป็นการง้อหรือป่าวน้าา แต่อยากให้เธอนะคะ</p>
                    <p className="line">จริงๆทางที่ดีคือเราควรต้องคุยต่อหน้ากันมากกว่า แต่เวลาคุยต่อหน้าเธอทีไรเค้าเรียบเรียงคำพูดไม่ค่อยถูก😅</p>
                    <p className="line">ตอนแรกเค้าว่าจะเขียนการ์ดมาแต่รู้สึกว่ามันอาจจะดูยาวเกินไป</p>
                    <p className="line">โอเค เค้าอดทนอยู่โดยที่ไม่มีเธอตามที่เธอบอกแล้วนะคือมันยากจริงๆคับ🥲</p>
                    <p className="line">เค้าใช้เวลาทบทวนตัวเองอยู่หลายวัน</p>
                    <p className="line">แต่พอนึกย้อนกลับไปแล้วทำให้รู้เลยคับ อย่างที่เธอบอกจริงๆนั่นแหละเธอพยายามมากๆ เธอทำทุกอย่างเท่าที่ทำได้</p>
                    <p className="line">เธอพยายามรักษาความสัมพันธุ์ของเรามาตลอดอยู่ฝ่ายเดียว</p>
                    <p className="line">ไม่รู้ว่าจะอธิบายยังไงแต่พอคิดทบทวนแล้ว เมื่อปีแรกๆเค้าไม่ได้รู้สึกรักเธอขนาดนี้ แต่พอเค้าเริ่มรักเธอขึ้นมาจริงๆ เค้ายังรู้จักแต่ความรักเด็กๆไปวันๆ นั่นแหละมันดูไม่โตอย่างที่เธอพูด</p>
                    <p className="line">ความคิดเขาที่ผ่านมาเค้าแค่รู้สึกเวลาเราคุยกันดีๆ ไม่นอกใจกัน ตอบแชทโทรคุยเป็นประจำหรือแค่มีเธออยู่ข้างๆ ได้ยินเสียงเธอได้เห็นหน้าเธอ เค้าก็มีความสุขแล้ว</p>
                    <p className="line">แล้วเค้าก็ยังคิดแค่ว่าเค้าไม่ออกไปไหน ไม่ไปเที่ยว ไม่ติดเพื่อน เค้าแค่นั่งทำงานนั่งเรียนไปวันๆแล้วก็รอคุยกับเธอแค่นั้น</p>
                    <p className="line">แต่มันกลับไม่ใช่เลยมันดันกลายเป็นการไม่ทำอะไรจริงๆคับ แค่รู้สึกว่าให้ข้าวฟ่างมาอยู่ข้างๆตัวเองจนมองข้ามความรู้สึกของเธอไปจริงๆเหมือนเธอยืนพิงท่อนไม้ยังงั้น อารมณ์แบบเหมือนเค้าไม่มีกิ่งก้านอะไรไปบังฝนบังแดดให้เธอเลยรู้แค่ว่าเธออยู่ข้างๆก็พอใจแล้ว</p>
                    <p className="line">พอเค้าลองคิดแล้วเค้าก็เข้าใจแล้วคับ ทั้งๆที่เค้าอยู่ข้างๆเธอ แต่ว่าเธอบอกเขาว่าเธอพึ่งอะไรเค้าไม่ได้เลยจนเธอต้องไปพึ่งคนอื่นแทน</p>
                    <p className="line">จริงๆเค้ารู้สึกเสียใจมากๆเวลาที่เธอเอาเค้าไปเปรียบเทียบกับคนอื่นว่า เค้าไม่เท่าคนนี้เค้าไม่เท่าคนนั้น</p>
                    <p className="line">คือพอเค้าคิดมาถึงตรงนี้เค้าก็ รู้สึกแย่จริงๆคับ ที่เค้าทำนิสัยเห็นแก่ตัว ที่จะรับจากเธออย่างเดียว🥹</p>
                    <p className="line">แล้วพอถึงวันที่เค้าต้องเป็นคนที่อยู่ข้างเธอจริงๆ กลับไม่สามารถรับอารมณ์อื่นๆของเธอได้เลย เพราะตัวเองได้รับร้อยยิ้ม เสียงหัวเรอะที่เธอพยายามทำให้เค้ามาตลอด</p>
                    <p className="line">หลายวันที่ผ่านมานี้ เค้าพยามคิดแล้วก็หาวิธีรักษาความสัมพันธ์ของเราแล้วนะคะ จริงๆเค้าอยากแก้มันมากๆ แต่เค้าจะยอมรับการตัดสินใจของเธอนะ เพราะที่ผ่านมาเค้าแย่จริงๆ เค้าอะไรให้เธอไม่ได้เลยอะ คือตอนนี้เค้าได้แค่จับมือเธอไว้เท่านั้นไม่กล้าที่จะดึงมือเธอมาด้วยซ้ำ</p>
                    <p className="line">เค้าพยายามจะยอมรับให้ได้เหมือนที่เธอบอกนะคะ แต่ทุกๆวันที่ผ่านมานี้เค้าก็กลัวอยู่ดีที่เธอจะสบัดมือเค้าออกไป แต่เค้ารู้สึกแบบนี้มากๆนะคับตอนนี้คือเค้าไม่อยากเสียเธอไป แต่มันคงยากแล้วใช่ไหมคะ เค้าเข้าใจเธอคับที่เธอจะมีเหตุผลไปจากเค้าจริงๆ</p>
                    <p className="line">เค้าไม่รู้จะพูดอะไรแล้วเลยแต่เค้าขอบคุณเธอมากๆเลยคับ คือช่วงเวลาหลายปีที่ผ่านมาคับเค้ามีความสุขมากๆ แล้วเค้าก็รักเธอมากขึ้นทุกปี โดยเค้าก็ไม่รู้เหมือนกันว่าทำไมเค้าถึงรักผู้หญิงคนนี้</p>
                    <p className="line">เค้าได้เรียนรู้จากเธอมาเยอะมากๆ เค้าคอยมองดูเธอตลอดนะคะแต่เค้าดันกลับไม่ทำอะไรเลยจนเธอต้องเรียกร้อง🥹 แต่เค้าก็อยากเอาสิ่งที่เรียนรู้แล้วก็ความผิดพลาดนี้มารักษาความสัมพันธ์ของเราต่อนะคับเค้ารู้ว่ามันเกิดรอยร้าวหรือสำหรับเธออาจจะแตกไปบางส่วนแล้วแต่เค้าก็อยากจะซ่อมมันจริงๆนะ</p>
                    <p className="line">อาจจะดูเห็นแก่ตัวอีกแล้วแต่เธอไม่ไปได้มั้ย</p>
                    <p className="line">เรื่องจริงนะคะที่เวลาเค้าเห็นเธอยิ้มเค้ามีความสุขมากๆ แต่ขอโทษจริงๆครับที่เค้าไม่เคยทำให้เธอยิ้มเลย เธอบอกเธอพยายามยิ้มเองแต่เขาก็อย่ากทำให้เธอยิ้มบ้างอะ</p>
                    <p className="line">คืนดีกันได้แล้วนะคะ เค้าให้เธอกระทืบเค้าเลยก็ได้ อยากระบายอะไร นัททีจะรับเอง</p>
                    <p className="line">มีต่อๆนะ</p>
                    <p className="line">.</p>
                    <p className="line">.</p>
                    <p className="line">.</p>
                </div>
            </div>

            {showChoices && (
                <div className="choices-container">
                    <div className="choices-prompt">คืนดีกันน้าาาาาา 💗</div>
                    <div className="choices glass" role="group" aria-label="Answer choices">
                        <button
                            className="choice-btn yes"
                            style={{ transform: `scale(${yesScale})` }}
                            onClick={onClickYes}
                        >
                            ได้ 💝
                        </button>
                        {noButtonVisible && (
                            <button
                                className="choice-btn no"
                                style={{ transform: `scale(${noScale})` }}
                                onClick={onClickNo}
                            >
                                ไม่ 😢
                            </button>
                        )}
                    </div>
                </div>
            )}

            <div className="confetti-layer" aria-hidden>
                {burst.map((b) => (
                    <span
                        key={b.id}
                        className={`confetti ${b.shape}`}
                        style={
                            {
                                "--x": `${b.x}vw`,
                                "--y": `${b.y}vh`,
                                "--r": `${b.r}deg`,
                                "--s": b.s,
                                "--d": `${b.d}s`,
                                "--dx": `${b.dx}px`,
                                "--color": b.color,
                            } as React.CSSProperties
                        }
                    />
                ))}
            </div>
        </div>
    );
}
